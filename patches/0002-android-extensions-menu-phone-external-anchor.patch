diff --git a/chrome/android/java/src/org/chromium/chrome/browser/ChromeTabbedActivity.java b/chrome/android/java/src/org/chromium/chrome/browser/ChromeTabbedActivity.java
index 46e151c155be911b6f828c0fdfcfc21ed7f4ba0a..c12f4b3e36ebd5bf8fd745bc7e88cb04cf0f6dac 100644
--- a/chrome/android/java/src/org/chromium/chrome/browser/ChromeTabbedActivity.java
+++ b/chrome/android/java/src/org/chromium/chrome/browser/ChromeTabbedActivity.java
@@ -4493,7 +4493,9 @@ public class ChromeTabbedActivity extends ChromeActivity implements PreAttachInt
             ExtensionsToolbarCoordinator coordinator =
                     getToolbarManager().getExtensionsToolbarCoordinator();
             if (coordinator != null) {
-                coordinator.showExtensionsMenu();
+                View popupAnchorView =
+                        mRootUiCoordinator.getAppMenuHandler().getLastAppMenuAnchorView();
+                coordinator.showExtensionsMenu(popupAnchorView);
                 RecordUserAction.record("MobileMenuExtensionsMenu");
             }
         } else if (id == R.id.manage_extensions_menu_id) {
diff --git a/chrome/android/java/src/org/chromium/chrome/browser/toolbar/ToolbarManager.java b/chrome/android/java/src/org/chromium/chrome/browser/toolbar/ToolbarManager.java
index ea58b7d46504fb7e25c1ebb9f8a8ccbbabb0b37d..15351cdbbc9dbdcd8c1c122d211fb2e7f8e8e689 100644
--- a/chrome/android/java/src/org/chromium/chrome/browser/toolbar/ToolbarManager.java
+++ b/chrome/android/java/src/org/chromium/chrome/browser/toolbar/ToolbarManager.java
@@ -2659,6 +2659,7 @@ public class ToolbarManager
                                                         mTabCreatorManager.getTabCreator(false),
                                                         getBrowsingModeThemeColorProvider(),
                                                         mToolbarLayout,
+                                                        !(mToolbarLayout instanceof ToolbarTablet),
                                                         contextMenuPopulatorFactory,
                                                         selectionDropdownMenuDelegate,
                                                         mTabModelSelector,
diff --git a/chrome/browser/ui/android/appmenu/internal/java/src/org/chromium/chrome/browser/ui/appmenu/AppMenuHandlerImpl.java b/chrome/browser/ui/android/appmenu/internal/java/src/org/chromium/chrome/browser/ui/appmenu/AppMenuHandlerImpl.java
index 9a4340102fdd817aa735d94474231816115921ce..841d879d42da4f7af5cbf0ee655ec3cf548cb52d 100644
--- a/chrome/browser/ui/android/appmenu/internal/java/src/org/chromium/chrome/browser/ui/appmenu/AppMenuHandlerImpl.java
+++ b/chrome/browser/ui/android/appmenu/internal/java/src/org/chromium/chrome/browser/ui/appmenu/AppMenuHandlerImpl.java
@@ -107,6 +107,7 @@ class AppMenuHandlerImpl
     private final List<AppMenuBlocker> mBlockers;
     private final List<AppMenuObserver> mObservers;
     private final View mHardwareButtonMenuAnchor;
+    private @Nullable View mAppMenuAnchorView;
 
     private final Context mContext;
     private final AppMenuPropertiesDelegate mDelegate;
@@ -241,6 +242,7 @@ class AppMenuHandlerImpl
                     .removeKeyboardVisibilityListener(mKeyboardVisibilityListener);
         }
         hideAppMenu();
+        mAppMenuAnchorView = null;
 
         mActivityLifecycleDispatcher.unregister(this);
     }
@@ -332,6 +334,7 @@ class AppMenuHandlerImpl
                 || !anchorView.getRootView().isAttachedToWindow()) {
             return false;
         }
+        mAppMenuAnchorView = anchorView;
 
         assert !(isByPermanentButton && startDragging);
 
@@ -459,6 +462,11 @@ class AppMenuHandlerImpl
         return mAppMenu != null && mAppMenu.isShowing();
     }
 
+    @Override
+    public @Nullable View getLastAppMenuAnchorView() {
+        return mAppMenuAnchorView;
+    }
+
     /**
      * @return The App Menu that the menu handler is interacting with.
      */
diff --git a/chrome/browser/ui/android/appmenu/java/src/org/chromium/chrome/browser/ui/appmenu/AppMenuHandler.java b/chrome/browser/ui/android/appmenu/java/src/org/chromium/chrome/browser/ui/appmenu/AppMenuHandler.java
index 49327f8c018c4521486027694a4b63481e81814d..a31c554e4aca27ed581d86ef84ff92ac60b148c7 100644
--- a/chrome/browser/ui/android/appmenu/java/src/org/chromium/chrome/browser/ui/appmenu/AppMenuHandler.java
+++ b/chrome/browser/ui/android/appmenu/java/src/org/chromium/chrome/browser/ui/appmenu/AppMenuHandler.java
@@ -177,6 +177,14 @@ public interface AppMenuHandler {
         return showAppMenu(anchorView, startDragging);
     }
 
+    /**
+     * @return The effective anchor used for the most recently shown app menu, or null if none is
+     *     available.
+     */
+    default @Nullable View getLastAppMenuAnchorView() {
+        return null;
+    }
+
     /**
      * @return A new {@link AppMenuButtonHelper} to hook up to a view containing a menu button.
      */
diff --git a/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsMenuCoordinator.java b/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsMenuCoordinator.java
index 957219a6303b2ddd5c2c6a615e65a1c7c59b0251..25513615ef012ff595eaf23fe870c15cd7954538 100644
--- a/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsMenuCoordinator.java
+++ b/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsMenuCoordinator.java
@@ -71,6 +71,8 @@ public class ExtensionsMenuCoordinator
     private final Context mContext;
     private final ListMenu mExtensionsMenu;
     private final ListMenuButton mExtensionsMenuButton;
+    private final boolean mUsesExternalMenuPopupAnchor;
+    private @Nullable View mExtensionsMenuPopupAnchorOverride;
     private final ThemeColorProvider mThemeColorProvider;
     private final NullableObservableSupplier<Tab> mCurrentTabSupplier;
     private final TabCreator mTabCreator;
@@ -102,6 +104,8 @@ public class ExtensionsMenuCoordinator
      *
      * @param context The context for this component.
      * @param extensionsMenuButton The puzzle icon in the toolbar.
+     * @param useExternalMenuPopupAnchor Whether the popup should use an external anchor supplied
+     *     when it is shown.
      * @param themeColorProvider The provider for theme colors.
      * @param task Supplies the {@link ChromeAndroidTask}.
      * @param windowAndroid The {@link WindowAndroid} for the current activity.
@@ -116,6 +120,7 @@ public class ExtensionsMenuCoordinator
     public ExtensionsMenuCoordinator(
             Context context,
             ListMenuButton extensionsMenuButton,
+            boolean useExternalMenuPopupAnchor,
             ThemeColorProvider themeColorProvider,
             ChromeAndroidTask task,
             WindowAndroid windowAndroid,
@@ -134,10 +139,17 @@ public class ExtensionsMenuCoordinator
         mExtensionsToolbarBridge = extensionsToolbarBridge;
         mMenuButtonPinningDelegate = menuButtonPinningDelegate;
         mModalDialogManager = modalDialogManager;
+        mExtensionsMenuButton = extensionsMenuButton;
+        mUsesExternalMenuPopupAnchor = useExternalMenuPopupAnchor;
 
         mExtensionsToolbarBridge.setMenuDelegate(this);
 
         mContentView = LayoutInflater.from(mContext).inflate(R.layout.extensions_menu, null, false);
+        if (mUsesExternalMenuPopupAnchor) {
+            mContentView
+                    .findViewById(R.id.extensions_menu_pin_menu_icon_button)
+                    .setVisibility(View.GONE);
+        }
 
         mExtensionsMenu =
                 new ListMenu() {
@@ -156,7 +168,6 @@ public class ExtensionsMenuCoordinator
                     }
                 };
 
-        mExtensionsMenuButton = extensionsMenuButton;
         mExtensionsMenuButton.setMenuMaxWidth(
                 context.getResources().getDimensionPixelSize(R.dimen.extension_menu_max_width));
         mExtensionsMenuButton.setDelegate(
@@ -168,18 +179,18 @@ public class ExtensionsMenuCoordinator
 
                     @Override
                     public RectProvider getRectProvider(View listMenuHostingView) {
-                        return MenuBuilderHelper.getRectProvider(mExtensionsMenuButton);
+                        View popupAnchorView = mExtensionsMenuPopupAnchorOverride;
+                        mExtensionsMenuPopupAnchorOverride = null;
+                        return MenuBuilderHelper.getRectProvider(
+                                popupAnchorView != null
+                                        ? popupAnchorView
+                                        : mExtensionsMenuButton);
                     }
                 },
                 /* overrideOnClickListener= */ true);
 
         // Menu mediator is created when menu is triggered.
-        mExtensionsMenuButton.setOnClickListener(
-                (view) -> {
-                    TrackerFactory.getTrackerForProfile(mProfile)
-                            .notifyEvent(EventConstants.EXTENSIONS_MENU_BUTTON_CLICKED);
-                    createMediator();
-                });
+        mExtensionsMenuButton.setOnClickListener((view) -> showMenu(/* popupAnchorView= */ null));
 
         mExtensionsMenuButton.addPopupListener(
                 new ListMenuHost.PopupMenuShownListener() {
@@ -224,6 +235,25 @@ public class ExtensionsMenuCoordinator
         mModalDialogManager.addObserver(mModalDialogManagerObserver);
     }
 
+    void showMenu(@Nullable View popupAnchorView) {
+        if (mUsesExternalMenuPopupAnchor) {
+            // Hardware-menu anchors may be zero-sized, so only require a valid attached view.
+            if (popupAnchorView == null
+                    || !popupAnchorView.isAttachedToWindow()
+                    || !popupAnchorView.isShown()
+                    || popupAnchorView.getWindowToken() == null) {
+                return;
+            }
+            mExtensionsMenuPopupAnchorOverride = popupAnchorView;
+        } else {
+            mExtensionsMenuPopupAnchorOverride = null;
+        }
+
+        TrackerFactory.getTrackerForProfile(mProfile)
+                .notifyEvent(EventConstants.EXTENSIONS_MENU_BUTTON_CLICKED);
+        createMediator();
+    }
+
     /**
      * Creates the extensions menu mediator and the associated JNI bridge, passing a runnable to
      * show the menu once the mediator has initialized the action.
diff --git a/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsMenuCoordinatorTest.java b/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsMenuCoordinatorTest.java
index 200cb8079161c034f99390a486f6a51824b7fe8f..7278d8635a39ffcf71d5e6a4e03b7a896269829e 100644
--- a/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsMenuCoordinatorTest.java
+++ b/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsMenuCoordinatorTest.java
@@ -156,6 +156,7 @@ public class ExtensionsMenuCoordinatorTest {
                 new ExtensionsMenuCoordinator(
                         mContext,
                         mExtensionsMenuButton,
+                        /* useExternalMenuPopupAnchor= */ false,
                         mThemeColorProvider,
                         mTask,
                         mWindowAndroid,
diff --git a/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsToolbarCoordinator.java b/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsToolbarCoordinator.java
index 5a04f265511c034551387567fb6cea21107387d8..cd511204da267665b53a1610fb68a12aab5f36be 100644
--- a/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsToolbarCoordinator.java
+++ b/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsToolbarCoordinator.java
@@ -6,6 +6,7 @@ package org.chromium.chrome.browser.toolbar.extensions;
 
 import android.content.Context;
 import android.view.KeyEvent;
+import android.view.View;
 import android.view.ViewGroup;
 import android.view.ViewStub;
 
@@ -55,6 +56,7 @@ public interface ExtensionsToolbarCoordinator extends ChromeAndroidTaskFeature,
             TabCreator tabCreator,
             ThemeColorProvider themeColorProvider,
             ViewGroup rootView,
+            boolean useExternalMenuPopupAnchor,
             @Nullable ContextMenuPopulatorFactory contextMenuPopulatorFactory,
             @Nullable SelectionDropdownMenuDelegate selectionDropdownMenuDelegate,
             TabModelSelector tabModelSelector,
@@ -80,6 +82,7 @@ public interface ExtensionsToolbarCoordinator extends ChromeAndroidTaskFeature,
                 tabCreator,
                 themeColorProvider,
                 rootView,
+                useExternalMenuPopupAnchor,
                 contextMenuPopulatorFactory,
                 selectionDropdownMenuDelegate,
                 tabModelSelector,
@@ -105,6 +108,7 @@ public interface ExtensionsToolbarCoordinator extends ChromeAndroidTaskFeature,
             TabCreator tabCreator,
             ThemeColorProvider themeColorProvider,
             ViewGroup rootView,
+            boolean useExternalMenuPopupAnchor,
             @Nullable ContextMenuPopulatorFactory contextMenuPopulatorFactory,
             @Nullable SelectionDropdownMenuDelegate selectionDropdownMenuDelegate,
             TabModelSelector tabModelSelector,
@@ -127,7 +131,7 @@ public interface ExtensionsToolbarCoordinator extends ChromeAndroidTaskFeature,
     void updateMenuButtonBackground(int backgroundResource);
 
     /** Shows the extensions menu programmatically. */
-    void showExtensionsMenu();
+    void showExtensionsMenu(@Nullable View popupAnchorView);
 
     /** Returns the {@link ToolbarWidthConsumer} for the popped out action. */
     ToolbarWidthConsumer getPoppedOutActionWidthConsumer();
diff --git a/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsToolbarCoordinatorImpl.java b/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsToolbarCoordinatorImpl.java
index f75f17b2c41e83ba477b35ffcc42ec95d5208004..9764633858df0541ab7e4e2274ca0068585d38f3 100644
--- a/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsToolbarCoordinatorImpl.java
+++ b/chrome/browser/ui/android/toolbar/java/src/org/chromium/chrome/browser/toolbar/extensions/ExtensionsToolbarCoordinatorImpl.java
@@ -82,6 +82,7 @@ public class ExtensionsToolbarCoordinatorImpl
 
     private boolean mCanShowMenuIcon = true;
     private boolean mShowExtensionsMenuPending;
+    private boolean mUsesExternalMenuPopupAnchor;
     private boolean mIsDestroyed;
     private final ExtensionsToolbarBridge.Observer mExtensionsToolbarBridgeObserver =
             new ExtensionsToolbarBridge.Observer() {
@@ -119,6 +120,7 @@ public class ExtensionsToolbarCoordinatorImpl
             TabCreator tabCreator,
             ThemeColorProvider themeColorProvider,
             ViewGroup rootView,
+            boolean useExternalMenuPopupAnchor,
             @Nullable ContextMenuPopulatorFactory contextMenuPopulatorFactory,
             @Nullable SelectionDropdownMenuDelegate selectionDropdownMenuDelegate,
             TabModelSelector tabModelSelector,
@@ -129,6 +131,7 @@ public class ExtensionsToolbarCoordinatorImpl
         mProfile = profile;
         mCurrentTabSupplier = currentTabSupplier;
         mOnFeatureRemoved = onFeatureRemoved;
+        mUsesExternalMenuPopupAnchor = useExternalMenuPopupAnchor;
 
         extensionsToolbarStub.setLayoutResource(R.layout.extensions_toolbar_container);
         mContainer = (LinearLayout) extensionsToolbarStub.inflate();
@@ -163,6 +166,7 @@ public class ExtensionsToolbarCoordinatorImpl
                 new ExtensionsMenuCoordinator(
                         context,
                         mContainer.findViewById(R.id.extensions_menu_button),
+                        useExternalMenuPopupAnchor,
                         themeColorProvider,
                         task,
                         windowAndroid,
@@ -315,23 +319,27 @@ public class ExtensionsToolbarCoordinatorImpl
     }
 
     @Override
-    public void showExtensionsMenu() {
-        mShowExtensionsMenuPending = true;
-        updateMenuIconVisibility();
-
+    public void showExtensionsMenu(@Nullable View popupAnchorView) {
         ListMenuButton extensionsMenuButton = mContainer.findViewById(R.id.extensions_menu_button);
         assert extensionsMenuButton != null;
 
-        // Post to click after the layout pass.
+        if (!mUsesExternalMenuPopupAnchor) {
+            mShowExtensionsMenuPending = true;
+            updateMenuIconVisibility();
+        }
+
+        @Nullable View effectivePopupAnchorView =
+                mUsesExternalMenuPopupAnchor ? popupAnchorView : null;
+        // Post to show after the app menu has dismissed and any layout pass has completed.
         extensionsMenuButton.post(
                 () -> {
                     mShowExtensionsMenuPending = false;
-                    extensionsMenuButton.performClick();
+                    mExtensionsMenuCoordinator.showMenu(effectivePopupAnchorView);
                 });
     }
 
     private void showIphInternal() {
-        if (mProfile.shutdownStarted()) return;
+        if (mUsesExternalMenuPopupAnchor || mProfile.shutdownStarted()) return;
 
         Activity activity = mWindowAndroid.getActivity().get();
         if (activity == null) return;
@@ -391,7 +399,7 @@ public class ExtensionsToolbarCoordinatorImpl
     }
 
     private void showPinnedByDefaultIphInternal(String extensionId) {
-        if (mProfile.shutdownStarted()) {
+        if (mUsesExternalMenuPopupAnchor || mProfile.shutdownStarted()) {
             return;
         }
 
@@ -486,6 +494,7 @@ public class ExtensionsToolbarCoordinatorImpl
     }
 
     private boolean shouldShowMenuIcon() {
+        if (mUsesExternalMenuPopupAnchor) return false;
         return mExtensionsMenuCoordinator.isExtensionsMenuOpen()
                 || mShowExtensionsMenuPending
                 || (mCanShowMenuIcon && isMenuButtonPinned());
